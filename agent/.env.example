# Which chain this process serves (one process per chain)
CHAIN=galileo

# Chain-independent agent secrets
AGENT_PRIVATE_KEY=0x...
AGENT_ECIES_PRIVATE_KEY=0x...
STORAGE_INDEXER=https://indexer-storage-testnet-turbo.0g.ai
ZG_SERVICE_URL=https://compute-network-6.integratenetwork.work
ZG_API_SECRET=...

# Galileo (CHAIN=galileo) - fill VAULT_ADDRESS + USDC_ADDRESS after deploying the v2 stack
GALILEO_RPC=https://evmrpc-testnet.0g.ai
VAULT_ADDRESS=0x...
USDC_ADDRESS=0x...

# Arbitrum Sepolia (CHAIN=arbitrum-sepolia) - fill after deploying there
ARBITRUM_SEPOLIA_RPC=https://sepolia-rollup.arbitrum.io/rpc
ARBITRUM_SEPOLIA_VAULT=0x...
# ARBITRUM_SEPOLIA_USDC defaults to Circle test USDC; override if needed

# Mainnet EVM chains (priceMode pyth) - usdc has a verified default; set the vault after deploy.
# Verify the USDC + the deepest pool fee tier on each explorer before deploying value.
# BASE_RPC=https://mainnet.base.org
# BASE_VAULT=0x...
# AVALANCHE_RPC=https://api.avax.network/ext/bc/C/rpc
# AVALANCHE_VAULT=0x...
# MANTLE_RPC=https://rpc.mantle.xyz
# MANTLE_VAULT=0x...
